import Meta from "@/components/Meta";

<Meta pageTitle="exec | bolt-framework" description="Run Docker containers, child processes, or even encapsulate your entire project within a VM, all under one unified API." />





# exec

This command opens up the VM shell for the project running in the bolt VM

```bash
$ bolt exec --help
Usage: bolt exec [options]

Opens up the VM shell for the project running in the VM

Options:
  -h, --help  display help for command
```
