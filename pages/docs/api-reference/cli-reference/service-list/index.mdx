import Meta from "@/components/Meta";

<Meta pageTitle="service:list | bolt-framework" description="Run Docker containers, child processes, or even encapsulate your entire project within a VM, all under one unified API." />





# service:list

This command lists all routes generated using "bolt.yaml" file's ingress object.

```bash
$ bolt service:list --help
Usage: bolt service:list [options]

Lists all services available in "bolt.yaml" file

Options:
  -h, --help  display help for command
```
