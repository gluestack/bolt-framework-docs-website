import Meta from "@/components/Meta";

<Meta pageTitle="project:info | bolt-framework" description="Run Docker containers, child processes, or even encapsulate your entire project within a VM, all under one unified API." />





# project:info

This command will print the project's information from bolt.yaml file.

```bash
$ bolt project:info --help
Usage: bolt info [options]

Prints the project's information from bolt.yaml file.

Options:
  -h, --help  display help for command
```
